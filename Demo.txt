resouces: https://git-scm.com/download/win

--clean screen
ctrl+L

--select directory
cd /D

--select folder
cd your_folder_name/

--configure git

git config --global user.name rezaice07

press ennter key

git config --global user.email rezaice07@gmail.com

press ennter key

ennter your password....

--clone git repository

git clone https://github.com/rezaice07/DLMS.git


--add file in git repository

git add file_name

or you can add multiple files


--check status
git status

--commit file
git commit -m 'Added Demo.txt' Demo.txt

--transfer file to git repository
git push -u origin master


--get update from master branch
git pull origin master

--creating a new branch
git checkout -b slave

--push your branch to github repository
git push -u origin slave

--switching into the branch
git switch slave

--commit modified files
git commit -am "Comment: Modified in demo.txt"
then 
git push

--deleting branch
git branch -D slave

